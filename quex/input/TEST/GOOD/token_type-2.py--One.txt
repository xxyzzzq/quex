-----------------------------
IN:
    [{ name : std::string; }]

OUT:

type(token_id)      = QUEX_TYPE_TOKEN_ID
type(column_number) = size_t
type(line_number)   = size_t
distinct members {
    std::string name
}
union members {
}

