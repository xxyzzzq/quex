-----------------------------
IN:
    [{ uint8_t : id; }]

OUT:

type(token_id)      = uint8_t
type(column_number) = 
type(line_number)   = 
distinct members {
}
union members {
}

-----------------------------
IN:
    [{ uint8_t : column_number; }]

OUT:

type(token_id)      = 
type(column_number) = uint8_t
type(line_number)   = 
distinct members {
}
union members {
}

-----------------------------
IN:
    [{ uint8_t : line_number; }]

OUT:

type(token_id)      = 
type(column_number) = 
type(line_number)   = uint8_t
distinct members {
}
union members {
}

