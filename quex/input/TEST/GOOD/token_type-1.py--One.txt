-----------------------------
IN:
    [{ id : uint8_t; }]

OUT:

type(token_id)      = uint8_t
type(column_number) = 
type(line_number)   = 
distinct members {
}
union members {
}

-----------------------------
IN:
    [{ column_number : uint8_t; }]

OUT:

type(token_id)      = 
type(column_number) = uint8_t
type(line_number)   = 
distinct members {
}
union members {
}

-----------------------------
IN:
    [{ line_number : uint8_t; }]

OUT:

type(token_id)      = 
type(column_number) = 
type(line_number)   = uint8_t
distinct members {
}
union members {
}

