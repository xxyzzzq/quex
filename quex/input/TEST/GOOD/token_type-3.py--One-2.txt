-----------------------------
IN:
    [{ 
                  { 
                     name : std::string; 
                     value : int; 
                  }
                }
             ]

OUT:

type(token_id)      = QUEX_TYPE_TOKEN_ID
type(column_number) = size_t
type(line_number)   = size_t
distinct members {
}
union members {
    {
        std::string name
        int         value
    }
}

