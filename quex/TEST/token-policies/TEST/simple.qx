header {
    // Helper to investigate stack unwinding
    #include <string>
    #include <iostream>
    class Unwinder {
        std::string  name;
    public:
        Unwinder(const std::string Name) : name(Name) {}
        ~Unwinder() { std::cerr << "Destructed " << name << std::endl; }
    };
}

token {
    ONE
    TWO
    THREE
    FOUR
    FIVE
    _____NEXT_____
}
// The mode is totally senseless, since we do all sending in 
// a pseudo-analysis function (see lexer.cpp)
start = ALMOST_EMPTY;
mode ALMOST_EMPTY : { a => QUEX_TKN_ONE; }
