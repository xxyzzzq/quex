-----------------------------
IN:
    [uint8_t : token_id; {]

OUT:

type(token_id)       = uint8_t
type(column_counter) = 
type(line_counter)   = 
header variables {
}

-----------------------------
IN:
    [uint8_t : column; {]

OUT:

type(token_id)       = 
type(column_counter) = uint8_t
type(line_counter)   = 
header variables {
}

-----------------------------
IN:
    [uint8_t : line; {]

OUT:

type(token_id)       = 
type(column_counter) = 
type(line_counter)   = uint8_t
header variables {
}

