start = X;
define {
    SUBST  ih
}
mode X {
    \R{dlrow} => QUEX_TKN_WORLD(LexemeNull);
}
mode Y {
    \R{[0-9][a-z]} => QUEX_TKN_WORLD(LexemeNull);
}
mode Z {
    A\R{abc}Z => QUEX_TKN_WORLD(LexemeNull);
}
mode Zb {
    \R{{SUBST}} => QUEX_TKN_WORLD(LexemeNull);
}
